import torch
import numpy as np
import torch.nn as nn
import torch.nn.functional as F
from relation_map import noun_number, noun_dim, relation_num, relation_map, lr_per_embedding



# def question(): 指令级别的提问，输入是一个问题，输出是一个答案
# def ensure_relation(model, relation_map, i, j, relation_type):
# what = similaityrity search
# where = type search
# when = type search



